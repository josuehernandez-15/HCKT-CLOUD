# ============================================================
# ALERTA UTEC - SERVERLESS COMPOSE
# ============================================================

org: ${env:ORG_NAME}
app: alerta-utec-backend
stage: dev

hooks:
  compose:deploy:before:
    - echo "📊 Ejecutando poblamiento..."
    - python3 DataGenerator/DataPoblator.py
    - echo "✅ Poblamiento completado"

services:
  # Capa compartida con dependencias Python
  dependencias:
    path: Dependencias

  # Microservicio de gestión de usuarios
  usuarios:
    path: Usuarios
    dependsOn:
      - dependencias

  # Microservicio de gestión de incidentes
  incidentes:
    path: Incidentes
    dependsOn:
      - dependencias
      - usuarios

  # Microservicio de analítica
  analitica:
    path: Analitica
    dependsOn:
      - dependencias
      - incidentes

